#include "main.h"
/**
 * _isdigit - check if digit.
 *
 *@a: first number.
 *@b: second number.
 *@c: multiplication of the two numbers.
 *
 * Return: 0 on success.
 */
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
