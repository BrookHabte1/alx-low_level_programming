#include "main.h"
/**
 *swap_int - swaps the valur of teo integers.
 *@x: digit.
 *Return: it eill not return anything./\
 */
void swap_int(int *a, int *b)
{
    int x = *a;
    *a = *b;
    *b = x;
}
